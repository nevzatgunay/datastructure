#ifndef STUDENT_H
#define STUDENT_H

#include <stdio.h>

#include "array.h"

struct Student{
	char *name;
	int id;
};

void print_student_list(struct Array *student_list);
FILE *open_phone_book_file(const char *filename);

#endif
